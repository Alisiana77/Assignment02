let x = window.prompt("Enter a number here!");
let y = window.prompt("Enter another number :)");
if (x > y) {
    window.document.write("Bigger# is " + x);
} else if (x < y) {
    window.document.write("Bigger# is " + y);
} else if (x == y) {
    window.document.write("You put same#");
}
